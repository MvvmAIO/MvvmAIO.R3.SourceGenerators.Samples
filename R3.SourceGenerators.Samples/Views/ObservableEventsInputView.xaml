<UserControl x:Class="R3.SourceGenerators.Samples.Views.ObservableEventsInputView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="360" d:DesignWidth="640"
             Loaded="OnLoaded"
             Unloaded="OnUnloaded">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Border x:Name="PointerCaptureHost"
                Grid.Column="0"
                Margin="0,0,12,0"
                Padding="12"
                Background="#140096D7"
                BorderBrush="#33000000"
                BorderThickness="1"
                CornerRadius="4">
            <TextBlock Text="Move the pointer here. ThrottleFirst(50 ms) is applied in the view model."
                       TextWrapping="Wrap"
                       VerticalAlignment="Center"
                       Foreground="#CCFFFFFF" />
        </Border>
        <StackPanel Grid.Column="1" VerticalAlignment="Top">
            <TextBlock Text="Pointer position" FontWeight="SemiBold" FontSize="16" />
            <TextBlock Text="{Binding MousePosition}"
                       Margin="0,8,0,0"
                       TextWrapping="Wrap"
                       FontFamily="Consolas"
                       Foreground="#CCFFFFFF" />
        </StackPanel>
    </Grid>
</UserControl>
